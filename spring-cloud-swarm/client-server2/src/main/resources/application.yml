spring:
  application:
    name: client-server2
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: client-server2
server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8090/eureka/
