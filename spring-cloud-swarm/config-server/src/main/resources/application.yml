server:
  port: 8082
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/love-mh-forever/spring-cloud-examples/tree/master/spring-cloud-swarm
          search-paths: conf
          username: love-dl-forever                                        # git仓库的账号
          password: a810095178
                                               # git仓库的密码
        native:
          search-locations: conf

  profiles:
    active: native

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8090/eureka/
